let resultado = Math.abs(-4);  // 4

resultado = Math.ceil(10.0000001);  // 11

resultado = Math.floor(10.9999999); // 10

resultado = Math.round(100.499999); // 100

resultado = Math.max(10, 20, 30, 100, 40, 50); // 100

resultado = Math.min(10, 20, 30, 100, 40, 50);  // 10

resultado = Math.random();

resultado = Math.round(Math.random());
console.log(resultado);